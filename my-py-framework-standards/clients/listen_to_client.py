from typing import List
from my_py_framework_standards.


class ListenToClient(BaseClient):
    def __init__(self, client_name: str):
        self.client_name = client_name


    @staticmethod
    def add_three(self, vars: List[]):
        total: int = 0
        for var in vars:
            total = total + var
        return total






